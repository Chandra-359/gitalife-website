import GalleryPageContent from "@/components/home/GalleryPageContent";

export const metadata = {
  title: "Gallery — Gita Life NYC",
  description:
    "Photos from our community — festivals, classes, Harinam, retreats, Govinda's kitchen — plus the live feed from @gitalifenyc.",
};

export default function GalleryPage() {
  return <GalleryPageContent />;
}
