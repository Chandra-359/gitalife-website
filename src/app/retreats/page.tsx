import RetreatsPageContent from "@/components/home/RetreatsPageContent";

export const metadata = {
  title: "Retreats — Gita Life NYC",
  description:
    "Quarterly weekend retreats in upstate New York — kirtan, scripture study, hikes, and community.",
};

export default function RetreatsPage() {
  return <RetreatsPageContent />;
}
